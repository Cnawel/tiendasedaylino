# ========================================================================
# .htaccess - Tienda Seda y Lino (Modo Depuración)
# ========================================================================
# VERSIÓN MÍNIMA PARA DEBUGGING - Solo funcionalidad esencial

# Deshabilitar listado de directorios
Options -Indexes

# Agregar .php automáticamente si el archivo existe
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteCond %{REQUEST_FILENAME}.php -f
    RewriteRule ^(.*)$ $1.php [L,QSA]
</IfModule>
